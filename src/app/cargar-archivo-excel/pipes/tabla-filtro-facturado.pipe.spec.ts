import { TablaFiltroFacturadoPipe } from './tabla-filtro-facturado.pipe';

describe('TablaFiltroFacturadoPipe', () => {
  it('create an instance', () => {
    const pipe = new TablaFiltroFacturadoPipe();
    expect(pipe).toBeTruthy();
  });
});
